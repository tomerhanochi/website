services:
  website:
    container_name: website
    build:
      context: .
      dockerfile: Containerfile
      target: project
    entrypoint:
      - /usr/bin/hugo
    command:
      - server
      - --bind
      - 0.0.0.0
    develop:
      watch:
        - path: .
          action: rebuild
          ignore:
            - .git
            - public
            - .hugo_build.lock
    ports:
      - 1313:1313
